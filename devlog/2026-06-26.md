# 开发日志 2026-06-26

## 完成事项
- [x] 补充 GitHub 项目 README
- [x] 新增 `.gitignore`，排除依赖、构建产物和运行数据
- [x] 新增 `.gitattributes`，统一 Git 仓库换行规则
- [x] 保留 `data/` 目录占位文件，避免提交本地同步内容
- [x] 初始化 Git 版本控制并准备首个提交
- [x] 按代码审查清单修复文件下载鉴权、敏感字段暴露、大文件上传、pending 队列、Electron IPC、Service Worker 缓存和前端 `innerHTML` 风险点
- [x] 将 `start-electron.bat` 改为相对路径
- [x] 将开机自启默认改为关闭，后续交给设置界面显式控制

## 遇到的问题
- 当前项目目录不是 Git 仓库，需要先初始化本地仓库。
- 项目目录包含 `node_modules/`、`dist/`、`data/` 和本地大文件，上传 GitHub 前必须通过 `.gitignore` 排除。
- Windows 环境会提示 LF/CRLF 转换，已通过 `.gitattributes` 固定规则。
- GitHub 远程仓库地址已提供并完成首个版本推送。
- 普通 Node.js 与当前 `better-sqlite3` 原生模块 ABI 不一致，接口级验证需使用 Electron 运行时。
- 由于当前已安装版 ClipSync 正在占用 9527 且应用有单实例行为，源码后台隐藏验证不稳定；已通过语法检查和 Electron 前台启动日志确认源码服务能启动到 9528。

## 待办事项
- [ ] 用手机浏览器复测审批、上传、预览和下载完整链路
- [ ] 后续增加设置界面，允许用户显式控制开机自启

## 下一步计划
完成跨设备人工回归测试后，准备下一轮体验优化。
